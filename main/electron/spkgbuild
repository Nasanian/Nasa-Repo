# description	: just a dependency required for electron-player
# depends	: nodejs

name=electron
version=9999
release=git-r1

build() {
  npm install -i electron@latest -g --force --prefix=$PKG/usr/
}

# description	: lolcat based on C. EXTREMELY fast.
# depends	: gcc

name=c-lolcat
version=git
release=r1
options=""
noextract=""
backup=""
source=""

build() {
  mkdir -pv $PKG/etc
  git clone https://github.com/jaseg/lolcat.git --depth 1 --jobs $(nproc)
  cd lolcat
  make
  make install
}

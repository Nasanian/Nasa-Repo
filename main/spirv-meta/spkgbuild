# description	: installs spirv-meta which is both spirv-cross and spirv-tools combined to be compiled against mpv-vk
# depends	: spirv-tools spirv-cross

name=spirv-meta
version=9999
release=git-r1

build() {
  mkdir -pv $PKG/etc
}

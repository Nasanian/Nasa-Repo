# description	: Xdelta version 3 is a C library and command-line tool for delta compression using VCDIFF/RFC 3284 streams.
# depends	: make gcc

name=xdelta
version=git
release=r2
options=""
noextract=""
backup=""
source=""

build() {
  git clone https://github.com/ChronoMonochrome/xdelta3.git --depth 1 --jobs $(nproc)
  cd xdelta3
  ./configure --prefix=/usr
	make
	make DESTDIR=$PKG install
}
